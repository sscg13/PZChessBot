name: PZShatranjBot Debug Compilation

on:
  push:
    branches: [ "main", "ci" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }} - ${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}

    steps:
    # - uses: actions/checkout@v4
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
    - name: Compile debug
      run: make -j4 debug
    - name: Check Debug
      run: |
        ./pzshatranjbot bench 6
        echo "debug OK, ran with no errors"
